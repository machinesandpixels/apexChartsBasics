// Chart Options
const options = {
    // chart: height, weight, color, type, background options
    // series: This is the data of our chart
};

// Init Chart
const chart = new ApexCharts(document.getElementById("#chart"), options);

// Render Chart
chart.render();